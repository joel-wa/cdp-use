# UI
- Change UI to include workflow controls like press and record
- Allow updating workflow variables and saving new versions to run
- Allow viewing and easy editing workflows
- Add some delays when executing steps in workflows to make sure things load well. Optionally record time in between execution of tool calls and show them in the workflow


# Webhook api
- Allow workflows to be run by calling an api
- Add an optional parameter for definite workflow output